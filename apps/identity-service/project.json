{
  "name": "identity-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/identity-service/src",
  "projectType": "application",
  "tags": ["scope:service", "type:identity"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/identity-service",
        "main": "apps/identity-service/src/main.ts",
        "tsConfig": "apps/identity-service/tsconfig.app.json",
        "assets": [
          "apps/identity-service/prisma/schema.prisma",
          {
            "glob": "**/*",
            "input": "apps/identity-service/src/generated/prisma",
            "output": "src/generated/prisma"
          }
        ]
      }
    },
    "lint": { "executor": "@nx/eslint:lint" },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": { "jestConfig": "apps/identity-service/jest.config.ts" }
    }
  }
}
